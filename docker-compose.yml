version: '3'

services:
  carriage:
    build: .
    environment:
      - NODE_ENV=production
    env_file:
      - server/.env
      - frontend/.env
    ports:
      - '3000:3000'
    restart: always
