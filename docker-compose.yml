version: '3'

services:
  carriage:
    build: .
    environment:
      - NODE_ENV=production
    env_file:
      - server/.env
      - frontend/.env
    ports:
      - '3001:3001'
    restart: always
